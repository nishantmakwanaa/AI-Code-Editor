"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const default_transport_config_1 = require("./default.transport.config");
class Transport {
    constructor(config) {
        this.configure(config || default_transport_config_1.defaultTransportConfig);
    }
    configure(config) {
        this.config = config;
    }
    send(message, messageConfig) {
        if (this.config.levels.includes(message.severity)) {
            return this.pass(this.config.format.getMessage(message), messageConfig);
        }
    }
    init() { }
    close() { }
}
exports.Transport = Transport;
//# sourceMappingURL=transport.js.map