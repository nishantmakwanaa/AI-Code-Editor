"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const format_1 = require("./format");
class JsonFormat extends format_1.Format {
    getMessage(logMessage) {
        const message = JSON.stringify(logMessage);
        const rowDeli = this.config.rowDelimiter || '';
        return `${message}${rowDeli}`;
    }
}
exports.JsonFormat = JsonFormat;
class LineFormat extends format_1.Format {
    getMessage(logMessage) {
        const message = (Object.values(logMessage))
            .join(this.config.columnDelimiter || ',');
        const rowDeli = this.config.rowDelimiter || '';
        return `${message}${rowDeli}`;
    }
}
exports.LineFormat = LineFormat;
class CsvFormat extends LineFormat {
    constructor(config) {
        super(config);
        this.config.columnDelimiter = ',';
    }
}
exports.CsvFormat = CsvFormat;
//# sourceMappingURL=formats.js.map