import { LogMessage } from '../messages/logMessage';
import { Format } from './format';
import { formatConfig } from './format.config';
export declare class JsonFormat extends Format {
    getMessage(logMessage: LogMessage): string;
}
export declare class LineFormat extends Format {
    getMessage(logMessage: LogMessage): string;
}
export declare class CsvFormat extends LineFormat {
    constructor(config?: formatConfig);
}
